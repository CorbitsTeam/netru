# Supabase Local Environment Variables
# Firebase Cloud Messaging v1 API Configuration

# Firebase Project ID (already configured in Supabase secrets)
FIREBASE_PROJECT_ID=netru-cefb8

# Firebase Service Account JSON (NEEDED - get from Firebase Console)
# Go to: https://console.firebase.google.com
# Select project: netru-cefb8
# Go to: Project Settings → Service accounts → Generate new private key
# Copy the entire JSON content here:
FIREBASE_SERVICE_ACCOUNT=REPLACE_WITH_ACTUAL_SERVICE_ACCOUNT_JSON

# INSTRUCTIONS:
# 1. Download firebase-service-account.json from Firebase Console
# 2. Copy its entire content (as single line) to replace REPLACE_WITH_ACTUAL_SERVICE_ACCOUNT_JSON
# 3. Run: npx supabase secrets set FIREBASE_SERVICE_ACCOUNT='paste-json-here'
# 4. Run: npx supabase functions deploy send-fcm-notification

# Legacy (not needed anymore with v1 API)
# FCM_SERVER_KEY=your_fcm_server_key_here

# Supabase Configuration
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: OpenAI API Key for Supabase AI features
OPENAI_API_KEY=your_openai_api_key_here

# ================================================
# How to get Firebase Service Account:
# ================================================
# 1. Go to Firebase Console: https://console.firebase.google.com
# 2. Select your project
# 3. Go to Project Settings (⚙️) → Service accounts
# 4. Click "Generate new private key"
# 5. Download the JSON file
# 6. Copy the entire JSON content and paste it as a single line in FIREBASE_SERVICE_ACCOUNT
# 7. Get your project ID from Project Settings → General → Project ID